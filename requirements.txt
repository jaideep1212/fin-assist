# Runtime dependencies — what the app needs to RUN.
# Test-only tools (pytest, httpx) live in requirements-dev.txt.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
python-dotenv>=1.0
apscheduler>=3.10
pyarrow>=15.0
APScheduler>=3.11,<4
azure-storage-blob