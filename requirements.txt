# Python dependencies for the fin-assist project.
# Pinned versions keep builds reproducible. Add new dependencies here as the
# project grows (e.g. database driver, Azure SDK).
pytest==8.3.4
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
python-dotenv>=1.0
apscheduler>=3.10
pyarrow>=15.0